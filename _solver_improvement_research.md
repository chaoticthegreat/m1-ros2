{
  "summary": "Research + adversarially-verify concrete improvements to the M1 position-only reach IK solver",
  "agentCount": 49,
  "logs": [
    "collected 43 candidate improvements from 5 angles",
    "2/43 proposals survived adversarial verification"
  ],
  "result": {
    "counts": {
      "candidates": 43,
      "verified": 43,
      "survivors": 2
    },
    "survivors": [
      {
        "angle": "performance",
        "title": "Cache FK/Jacobian results in _solve_from to eliminate redundant recomputation",
        "location": "_solve_from, _stack, _errs, position_jacobian",
        "effort": "medium",
        "confidence": "high"
      },
      {
        "angle": "structure",
        "title": "Position-only null-space prioritization: Exploit 4-DOF redundancy per arm for smoother singularity handling",
        "location": "_dls / _solve_from",
        "effort": "small",
        "confidence": "medium"
      }
    ],
    "report": "The code confirms the verdicts. Two candidates were judged `real=true` (#27 and #37), both with `would_regress=true`. Let me confirm the line-715/722 redundant FK claim in #27 and verify the per-DOF null-gain broadcast for #37, which I've already seen at lines 723-724. The code matches all the structural claims cited. I have enough to synthesize.\n\nA note on framing: the report asks for \"Top recommendations\" that survived verification (real=true, prefer would_regress=false). **Zero candidates** have `would_regress=false` — every single one is flagged `would_regress=true`. Only two are even `real=true` (#27, #37), and both regress. I'll report this honestly rather than manufacture recommendations.\n\n---\n\n# M1 Position-Only IK Solver — Improvement Triage\n\n**Bottom line:** Of 43 candidates, **none cleared the bar for an unqualified recommendation.** Every proposal carries `would_regress=true`. Only two are even judged genuine (`real=true`): #27 (redundant-FK caching) and #37 (per-DOF null-space weighting) — both still flagged as likely regressions. So the honest \"Top recommendations\" list is the two real-but-risky items, gated hard behind the test suite, with explicit kill criteria. The remaining 41 are rejected as either not-real or clearly-regressing.\n\n## Top recommendations\n\nThere are no clean wins. The two items below are the only ones grounded in a real code fact; both must be treated as **prototype-and-gate-test, revert-on-regression**, not merge-on-sight.\n\n### 1. Eliminate the one redundant FK per tracking iteration (from #27, scoped down)\n- **What to change:** In `_solve_from`'s `line_search=True` path, `_errs(q)` at line 715 computes FK, then line 722 `_stack(q,...)` immediately recomputes the same FK via `position_jacobian → fk`. Reuse the FK/error from the prior accepted iterate instead of recomputing it at the top of the next iteration. **Do NOT** build the proposed generic `tuple(q_vec)`-keyed cache — the verifier showed the hashing of a 15-element vector can cost more than the FK it saves, and the vague invalidation is the actual regression vector.\n- **Why it helps:** Saves exactly 1 FK chain-walk per GN iteration on the tracking path only. Real, but small (~1 FK × 2–3 iters/tick).\n- **Expected benefit:** Sub-0.5 ms per warm tracking tick — *not* the 3–5 ms the proposal claimed (a ~10x overestimate; FK is vectorized NumPy at ~0.1–0.2 ms). Treat any benefit as marginal and benchmark-noise-adjacent.\n- **Effort:** Small if done as a direct value-reuse (carry `e`/`dist` forward); medium and not worth it if done as the proposed cache.\n- **Biggest risk + de-risk:** A stale FK reused after `q` was clipped (line 733) corrupts the step and breaks sub-mm accuracy. De-risk: reuse only the FK of the *accepted* post-line-search iterate (`q, e, dist` are already updated together at line 737), never a pre-clip value. **Gate to watch:** single-arm sub-mm and held-arm <2mm (Gate B/C). If mean latency doesn't drop measurably above noise, drop it — the complexity isn't worth a wash.\n\n### 2. Per-DOF null-space weighting: lighter shoulder/elbow, stronger wrist (#37)\n- **What to change:** In `solve_step`'s `cold_gain` construction (~line 987), keep `cold_gain[lift_idx]=0.0`, then per arm set `cold_gain[i0:i0+4] *= 0.8` and `cold_gain[i0+4:i0+7] *= 2.0`. The element-wise `null_gain * (null_target - q)` broadcast at line 724 already applies per-DOF weights with **no solver change** — verified in code.\n- **Why it helps:** Pulls the wrist toward mid-range more firmly (avoiding contorted wrist postures and easing wrist-singularity crossings) while relaxing the shoulder/elbow pull that fights reach. Held arms are unaffected (their gain is reset uniformly downstream), and extreme targets are still rescued by the pure-task restart probes (`probe_gain[free]=0.0`, line 925).\n- **Expected benefit:** Claimed ~1 tick faster settle and smoother singularity traversal. **Unvalidated** — the verifier rated this `conf=medium`, the single genuinely uncertain (not clearly-bad) item in the set.\n- **Effort:** Small (a few lines, mechanically correct, trivially revertible).\n- **Biggest risk + de-risk:** Stronger wrist pull (0.08 vs 0.04) can make the primary undershoot targets needing extended wrist, triggering more restart probes and inflating `_IK_COLD_BUDGET` usage → p99 latency creep; in dual-arm, asymmetric per-arm wrist pull can break the shared-lift symmetry and raise per-arm error. De-risk: start conservative (e.g. wrist ×1.5, not ×2.0), and **gate on `test_dual_track` (dual-track p95 <8mm / max <20mm), `test_cold_hard`, and p99 solve_step <16.7ms** before committing. Revert if restart frequency rises.\n\n## Quick wins\n\n**None qualify.** Every \"trivial\"/\"small-effort\" candidate (#4, #7, #9 part, #13, #24, #29, #31, #33) was judged either not-real or clearly-regressing — several with outright math errors (#7 has the damping-direction backwards; #33 reduces damping where it must increase). Do not ship any of them as a \"while we're here\" change. The closest thing to a quick win is recommendation #2 above, and even it needs gate-testing.\n\n## Higher-risk / speculative\n\nThese have conceptual merit but the verifier flagged a concrete failure mode for each — pursue only with a dedicated A/B harness:\n\n- **#0 SDLS per-singular-value damping in `_dls`** — sound in the literature, but the proposed hard on/off at `IK_SV_EPS=0.04` is discontinuous (current ramp is smooth). *Caveat:* 60–75% estimated probability of regressing ≥1 gate, concentrated in dual-arm coordination; no smoothing/floor specified despite being named as the required mitigation.\n- **#1 Woodbury/cached SVD on the tracking path** — *Caveat:* stale SVD yields a stale descent direction + stale null-space projector; line-search rejects it (adding latency, not removing it), and Frobenius-norm staleness doesn't guard rank changes at `_IK_RANK_TOL`.\n- **#37 (also listed above as the least-bad bet)** — included here too because its `conf=medium` rating means it genuinely could go either way; the dual-arm symmetry break is the thing most likely to bite.\n- **#28 Block-decoupled dual-arm SVD** — *Caveat:* averaging two independent 3×8 lift steps is **not** the least-squares optimum of the coupled 6×15 system; introduces systematic shared-lift bias that threatens the strict `dual 100% <5mm` gate. Large effort for an accuracy-for-latency trade that likely fails the gate.\n\n## Rejected\n\n- **#1** — stale SVD breaks the line-search descent guarantee; FLOP savings ~50–150x smaller than claimed.\n- **#2** — 2-sample \"quadratic\" fit is underspecified; clamp-to-`alpha` can re-test the just-failed step, *adding* LS iterations near singularities.\n- **#3** — algorithmically wrong: pinned-arm Jacobian columns depend on the shared lift (`axis_w × (tip - p_w)`), so they're invalid the moment the lift moves; cache is always stale.\n- **#4** — asymmetric tolerance (primary exits at 0.005m, final gate still 0.001m) creates a state-consistency bug; probes run pure-task and don't auto-tighten.\n- **#5** — reducing null_gain while damping grows creates a non-linear \"step-size squeeze\" → oscillation/early stall; problem already handled by restart probes.\n- **#6** — measured pose is already seed #1; ±0.2 rad perturbation can't escape a singularity needing a branch flip; tie-break can lock the stuck basin.\n- **#7** — **math backwards:** cubic ramp applies *more* damping than quadratic for all `s_min < IK_SV_EPS`, slowing convergence (the opposite of the claim).\n- **#8** — single-arm condition number is decoupled from the shared-lift coupling and doesn't predict boundary distance; adds per-arm SVD per iteration (latency) and contradicts existing held-arm pinning.\n- **#9** — task-row weighting in a stacked LS system ≠ per-arm prioritization; conflicts with `IK_LIFT_HOLD_GAIN` anchor; strict `dual <5mm` gate at risk from sensitivity-metric noise.\n- **#10** — masks the null-space *before* projection (`N @ masked`), distorting the null-space basis; asymmetric silencing breaks the dual-arm lift compromise.\n- **#11** — evaluates condition of a 3×7 per-arm Jacobian, not the stacked system actually solved; 5–10 ms pre-scan eats 30–60% of budget.\n- **#12** — **wrong row indexing:** `range(0, 3*len(arms), 3)` selects X-rows (lift signal lives in Z-rows [2,5]); norm reads 0 → spurious full damping every iteration.\n- **#13** — misdiagnosed: held-arm dev is already 0.00mm (dual) / 9.8mm (far-jump, gate 10mm); tightening probe pinning over-constrains the shared-lift compromise.\n- **#14** — condition-number tie-break inverts the `_IK_CONTINUITY_BAND` design intent (proximity), risking snaps to distant branches; epsilon 1e-12 misaligns with `_IK_RANK_TOL` 1e-6.\n- **#15** — mutating `job[\"null_target\"]` to the moving `q_meas` every tick makes the null-space pull oscillate; `test_cold_then_nudge` already passes, so it \"fixes\" a non-problem and adds jitter.\n- **#16** — step expansion `alpha>1.0` breaks GN monotone descent; improvement metric fires most aggressively near the goal where it should be most conservative.\n- **#17** — misdiagnosed: 18–22mm primary undershoot is the *intended* posture-on intermediate that pure-task probes fix; replacing random seeds loses workspace coverage.\n- **#18** — hardcoded rank<5 is meaningless for the rank-≤6 dual system; 0.01m residual gate can't distinguish transient singularity dips from true unreachability; referenced skip-probe infra doesn't exist.\n- **#19** — `_dls` doesn't return `lam2` (needs a 2nd SVD or refactor, not \"2–3 conditionals\"); aggressive 0.3 shrink undershoots tight final approaches → fails sub-mm gates.\n- **#20** — line 849 already skips probes at ≤0.005m for *all* paths; the stricter 2mm re-acquire-only gate creates a semantic inconsistency that can trap a 2–5mm re-acquire in a bad branch.\n- **#21** — switching seeds mid-solve leaves a half-converged warm start, breaking the amortized-restart contract and the dual-arm posture-coherence that resolves the lift.\n- **#22** — single-arm lift bracketing doesn't predict the coupled dual-arm compromise; 6 pre-solves consume 67–100% of `_IK_COLD_BUDGET`; ignores the held arm's lift constraint.\n- **#23** — double-damping (SVD denominator already collapses near singularities) plus a discontinuity at `IK_SV_EPS`; the line search / stall detection already handle the symptom.\n- **#24** — incomplete plumbing of the `re_acquire` flag; if mis-scoped, cold solves snap to distant branches; the load-bearing mode boundary isn't specified.\n- **#25** — post-hoc per-arm `s_min<0.01` check fires on valid near-boundary/transient-singular converged states; competes with the proven residual-driven re-acquire.\n- **#26** — soft penalties (radians² penalty vs meters² cost) create valleys that trap the pure-task probes (`probe_gain[free]=0.0`) which *need* to saturate to reach extreme targets.\n- **#27** — *real bug found* (redundant FK at 715/722) but benefit overestimated ~10x; the proposed `tuple(q_vec)` cache + vague invalidation is the regression vector. (Scoped-down version promoted to Top rec #1.)\n- **#28** — block-decoupled lift averaging isn't the coupled least-squares optimum; systematic shared-lift bias threatens `dual <5mm`. (Also in Speculative.)\n- **#29** — seed-list size depends on `joint_order` (8 vs 15); memoizing one size breaks arm-set toggles (`test_stress` × 600); also freezes RNG diversity.\n- **#30** — adding `probe_boost` to the per-call budget term violates the `_IK_COLD_BUDGET=18` invariant → worst-case latency gate; contradicts the existing line-850 seed-clear for easy targets.\n- **#31** — skip threshold (5–8mm) meets/exceeds the `dual 100% <5mm` gate; skips the pure-task probes that are the *only* path reaching boundary targets below 5mm.\n- **#32** — replaces the hard 3×7 post-solve lift-frozen hold correction with weaker soft regularization → held-arm <2mm gate at risk; latency claim overstated ~5–10x.\n- **#33** — **damping direction backwards:** the formula reduces `lam2` everywhere including near singularities (75% reduction at `s_min=0.005`); M1's Jacobian is rank-≤3 so it fires on every solve.\n- **#34** — FK tip-distance is an unreliable reachability proxy for a redundant arm; pruning to top-6 seeds can drop the one branch that reaches a boundary target; no fallback re-queue exists.\n- **#35** — FK has branching control flow (not NumPy-broadcastable); the only real lever is distance-based seed filtering, which is the same unsound ranking as #34.\n- **#36** — lift-from-XY-reach ignores the target's Z entirely; produces badly-wrong warm seeds exactly on the extreme high/low targets it targets.\n- **#38** — two independent 2D compromises ≠ the full 15-DOF least-squares lift; 15–50 upfront sub-solve iterations exhaust the budget and delay convergence; held arm drifts.\n- **#39** — decoupling the warm-track solve breaks the simultaneous LS that resolves the shared lift; the decoupling benefit is *already* achieved correctly at the command stage (lines 1099–1117).\n- **#40** — fixed 3×3×3 grid creates zero-probability zones for unsampled configs (boundary-target reachability risk) and removes the random diversity that, with the proximity tie-break, is why the solver hits sub-mm.\n- **#41** — **damping scaling backwards:** large residual → *more* damping (not less); single stacked `lam2` can't tune per-arm, destabilizing the held arm; cold path has no line search to catch overshoot.\n- **#42** — caching the 3×7 hold Jacobian across ticks goes stale (position-dependent even within 0.05 rad of joint motion); cost overestimated ~10x; risks the held-arm gate via unstable hold feedback."
  }
}